{% set route = app.request.attributes.get('_route') %}
<nav class="navbar navbar-expand-lg bg-info">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link {% if 'home' in route %}fw-bold text-light{% endif %}  " aria-current="page" href="{{path('home')}}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if 'offer' in route %}fw-bold text-light{% endif %}" href="{{path('offer_locations')}}">Nos offres</a>
        </li>
        <li class="nav-item">
          <a class="nav-link {% if 'newsletter' in route %}fw-bold text-light{% endif %}" href="{{path('newsletter_subscribe')}}">Notre newsletter</a>
        </li>
      </ul>
    </div>

      <!-- SEARCH FIELD // NOT WORKING -->
    <form method='get' class="d-flex" role="search">
      <input class="form-control me-2" name="search" type="search" placeholder="" aria-label="Search">
      <button class="btn btn-light" type="submit">Rechercher</button>
    </form>
    
  </div>
</nav>